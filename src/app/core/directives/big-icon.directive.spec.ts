import { BigIconDirective } from './big-icon.directive';

describe('BigIconDirective', () => {
  it('should create an instance', () => {
    const directive = new BigIconDirective();
    expect(directive).toBeTruthy();
  });
});
